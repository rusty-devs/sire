# This sample manifest file should contain all of the require and
# optional properties which are supported by the manifest file
# parser so we can successfully parse all the data from it
project_name: "test-project"
project_description: "A description."
repository: "https://github.com/namespace/repo"
homepage: "http://example.com"
full_name: "Jane Doe"
license: "MIT"
version: "0.1.0"
email: "foo.bar@example.com"
extras:
  organization: "Fizz Buzz"
  build_mode:
    - foo
    - bar
  debug: true